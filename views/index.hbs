<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script type="text/javascript">
      $(document).ready(function(){

          $('button#takephoto').click(function(ev){
            
            $.post( "/takephoto", function( data ) {
              console.log(data);
              if(data.success){
                alert("TAKING PHOTO!");
              } else {
                alert("OOPS I BROKE!");
              }
            });

          });

      });
    </script>
  </head>
  <body>
    <h1>Welcome to {{title}}</h1>
    <p>Click the button to trigger the photo:</p>
    <button id="takephoto">CAPTURE THA BEAUTY!</button>
  </body>
</html>
